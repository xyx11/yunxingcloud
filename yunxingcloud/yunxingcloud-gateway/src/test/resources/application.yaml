spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
    sentinel:
      enabled: false
    gateway:
      enabled: true

logging:
  level:
    root: WARN
